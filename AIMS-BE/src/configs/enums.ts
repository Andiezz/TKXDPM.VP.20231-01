export enum COVER_TYPE {
    PAPER_BACK = 'paper back',
    HARD_COVER = 'hard cover',
}

export enum USER_ROLE {
    ADMIN = 'admin',
    MANAGER = 'manager',
}

export enum KIND {
    BOOK = 'book',
}